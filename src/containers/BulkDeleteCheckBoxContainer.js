import BulkDeleteCheckbox from '../common/components/BulkDeleteCheckbox'
import {connect} from 'react-redux'

const mapStateToProps=state=>({
    // data:state.bulkEdit
})
const mapDispatchToProps=dispatch=>({


})
export default connect(mapStateToProps,mapDispatchToProps)(BulkDeleteCheckbox)
